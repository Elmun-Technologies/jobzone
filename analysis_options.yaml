# Static analysis configuration. See https://dart.dev/tools/analysis
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - lib/localization/generated/**
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.mocks.dart"

linter:
  rules:
    # flutter_lints provides a strong default set; project-specific overrides
    # go here as the codebase grows.
